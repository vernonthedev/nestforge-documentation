---
title: Installation
description: Install the NestForge CLI, create an application, and run it locally.
---

# What this page is for

This page is for application developers using NestForge to create and run a service.

If you want to work on the NestForge framework repository itself, use the framework
developer setup in the contributing docs instead.

# Prerequisites

To create and run a NestForge app you need:

- a stable Rust toolchain
- `cargo`
- `git`

# Install the CLI

Install the CLI from Cargo:

```bash
cargo install nestforge-cli
```

If you are developing against a local checkout of the framework, you can install the
CLI from the repository workspace instead:

```bash
cargo install --path crates/nestforge-cli
```

# Create a project

After the CLI is installed, scaffold a new project:

```bash
nestforge new demo-api
cd demo-api
cargo run
```

# Run the app

The generated app starts with the normal Rust workflow:

```bash
cargo run
```

Typical local default for an HTTP service:

- `http://127.0.0.1:3000`

# Add the features you need

NestForge is modular at the Cargo level. Enable only the feature sets your application
needs. Common examples:

```toml
nestforge = { version = "1", features = ["config", "openapi"] }
nestforge = { version = "1", features = ["graphql"] }
nestforge = { version = "1", features = ["grpc", "microservices"] }
```

Depending on your stack, you may also need:

- `protoc` for gRPC code generation
- a reachable database for database-backed application code

# Next step

Continue with [CLI Workflow](/getting-started/cli-workflow/) or go straight to
[Quick Start](/getting-started/quick-start/).
